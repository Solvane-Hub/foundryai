/**
 * Centralized shared interfaces (Engineering Standards §5).
 *
 * database.ts is GENERATED from the live Supabase schema in Phase 1
 * (`npm run db:types`) and must never be hand-edited.
 */
export type {};
